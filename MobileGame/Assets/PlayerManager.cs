using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerManager : MonoBehaviour
{
    public int birdsCollected;
    // Start is called before the first frame update
    void Start()
    {
        birdsCollected = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
